# Offline dataset-augmentation pipeline dependencies.
# NOT part of the deployed backend (see ../requirements.txt) — install
# separately only when you need to (re)run the prediction pipeline.

torch>=2.2.0
torch_geometric>=2.5.0
openai>=1.30.0
transformers>=4.40.0
requests>=2.31.0
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
tqdm>=4.66.0
